import { Navbar, NavLink, NavItem } from 'react-bootstrap';

const Nav = () => {
  return (
    <Navbar>
      <NavItem>
        <NavLink>Home</NavLink>
      </NavItem>
    </Navbar>
  );
};

export default Nav;
